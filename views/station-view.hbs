{{> menu}}

<section class="section">
  <div class="title">
    {{station.name}}
  </div>
  {{> list-latest-readings}}
  {{> list-readings}}
  {{> add-reading}}
  {{> auto-generate-reading}}
</section>