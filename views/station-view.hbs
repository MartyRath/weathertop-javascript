{{> menu}}

<section class="section">
  <div class="title">
    {{station.name}}
  </div>
  {{> list-latest-readings}}
  {{> auto-generate-reading}}
  {{> chart}}
  {{> list-readings}}
  {{> add-reading}}
  
</section>